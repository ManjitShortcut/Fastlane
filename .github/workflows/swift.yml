name: Swift

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  UnitTest:
    runs-on: macOS-latest
    steps:
    - uses: actions/checkout@v1
    - name: List available Xcode versions
      run: ls /Applications | grep Xcode
    - name: Select Xcode
      run: sudo xcode-select --switch /Applications/Xcode_12.4.app/Contents/Developer
    - name: Available simulators
      run: xcrun simctl list --json devices available
    - name: Available sdks
      run: xcodebuild -showsdks
    - name: Boot iOS simulator
      run: xcrun simctl boot 6E84FD00-44FB-4675-AAFC-4E81A6E9BADB
    - name: Build for iOS
      run: xcodebuild build-for-testing -scheme FastLane_Integration -project FastLane_Integration.xcodeproj -destination "platform=iOS Simulator,OS=14.4,name=iPhone 12 Pro" | xcpretty && exit ${PIPESTATUS[0]}
    - name: Run iOS tests
      run: xcodebuild test-without-building -scheme FastLane_Integration -project FastLane_Integration.xcodeproj -destination "platform=iOS Simulator,OS=latest,name=Any iOS Device" | xcpretty && exit ${PIPESTATUS[0]}


